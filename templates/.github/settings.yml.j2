---
repository:
  description: Install and configure {{ role_name }} on a system.
  homepage: https://github.com/tehtbl/ansible-role-{{ role_name }}
{# this comment is needed for having a newline at EOF to pass yamllint tests #}
